[project]
name = "dsa-practice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rupesh Patel", email = "datasci.patel.rupesh@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
dsa-practice = "dsa_practice:main"

[build-system]
requires = ["uv_build>=0.11.14,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff>=0.15.16",
]
